name: 'Update info'
description: 'Update info file'

inputs:
  test-results:
    required: true
  user-test-info:
    required: true
  github-workspace:
    required: true

outputs:
  hasFailed:
    description: 'True or false'

runs:
  using: 'node16'
  main: 'index.js'